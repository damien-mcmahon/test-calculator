import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

describe('App', () => {
  it.todo('renders the calculator');
  it.todo('adds two numbers together')
});
